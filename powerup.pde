class powerup {
  short posX; //posX (width)
  short posY; //posY (height) von Powerup
  short difficulty; //Schwierigkeit
  String levelDifficulty; //Powerspawn Negativ + Postiv oder eins von beiden
  String powerupOptions[2][5]; //[0][i]Positive und [1][i]Negative Powerups
  String powerupChoose; //Powerup das gewählt wurde

  powerupOptions[0] = {"armor", "life", "speed", "shotSpeed", "shotBounce"};
  powerupOptions[1] = {"slowness", "lifeSickness", "playerColorSwitch"};

  void position(){
    p1.player
  }

  powerupChoose = "armor";

  switch (powerupChoose) {
    case
  }
}
